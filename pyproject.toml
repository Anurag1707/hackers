[project]
name = "ai-study-openenv"
version = "1.0.0"
description = "AI Study Assistant for OpenEnv Hackathon"
dependencies = [
    "flask",
    "openenv-core>=0.2.0"
]

# 🔥 IMPORTANT (fixes your error)
[project.scripts]
server = "server.app:main"